<script server>
  const timestamp = new Date().toISOString()

  meta('title', 'Datastar SPA Detail')
  meta('description', 'Datastar SPA detail sample')
</script>

<article class="space-y-2">
  <p class="text-xs font-semibold uppercase tracking-[0.14em] text-stone-500">SPA detail</p>
  <h3 class="text-xl font-semibold text-stone-950">Layout 없는 leaf render</h3>
  <p class="text-sm leading-6 text-stone-600">Datastar 요청에서는 leaf page 내용만 렌더되고, 플러그인이 selector patch로 감싸서 내려줍니다.</p>
  <p class="text-xs text-stone-500"><%= timestamp %></p>
</article>
