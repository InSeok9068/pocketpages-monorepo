<% const isProduction = String(env('APP_ENV') || 'development').trim() === 'production' %>
<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1" />
    <title><%= meta('title') %></title>
    <meta
      name="description"
      content="<%= meta('description') %>" />
    <meta
      name="theme-color"
      content="#f3f5f1" />
    <link
      rel="icon"
      type="image/svg+xml"
      href="<%= asset('/assets/favicon.svg') %>" />
    <link
      rel="apple-touch-icon"
      href="<%= asset('/assets/icon-192.png') %>" />
    <link
      rel="manifest"
      href="<%= asset('/assets/manifest.webmanifest') %>" />
    <link
      rel="stylesheet"
      href="<%= asset('/assets/style.css') %>" />
    <style>
      [x-cloak] {
        display: none !important;
      }
    </style>
    <%- include('unocss-head.ejs', { isProduction }) %>
    <script
      defer
      src="<%= asset('/assets/vendor/alpine-3.15.11-cdn.min.js') %>"></script>
    <%- slots.head %>
  </head>

  <body
    un-cloak
    class="m-0 min-h-screen bg-[#f3f5f1] text-[#263028] antialiased">
    <main class="mx-auto min-h-screen max-w-lg px-5 py-5 sm:px-6 sm:py-8"><%- slots.body || slot %></main>
  </body>
</html>
