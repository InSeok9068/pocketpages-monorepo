<script server>
  meta('title', 'Booklog')
  meta('description', 'Booklog 기본 메인 페이지')

  const flashMessage = params.__flash ? String(params.__flash) : ''
</script>

<section class="space-y-4">
  <p class="text-sm font-medium uppercase tracking-[0.18em] text-stone-500">PocketPages Booklog</p>
  <h1 class="text-4xl font-semibold tracking-tight text-stone-950">Booklog</h1>
  <p class="max-w-2xl text-base leading-7 text-stone-600">로그인 기능을 붙이기 전 단계의 기본 메인 페이지입니다.</p>
</section>

<% if (flashMessage) { %>
<section class="mt-6 rounded-2xl border border-stone-200 bg-white px-5 py-4 text-sm text-stone-700 shadow-sm">
  <%= flashMessage %>
</section>
<% } %>

<section class="mt-8 rounded-3xl border border-stone-200 bg-white p-6 shadow-sm">
  <div class="space-y-2">
    <h2 class="text-2xl font-semibold text-stone-950">EPUB Upload Test</h2>
    <p class="text-sm leading-6 text-stone-600">EPUB 파일 하나를 올리면 서버가 파일을 읽고 내부에서 찾은 문자열과 엔트리 이름을 로그로 남깁니다.</p>
  </div>

  <form class="mt-6 space-y-4" method="post" action="/xapi/epub/upload" enctype="multipart/form-data">
    <label class="block space-y-2">
      <span class="text-sm font-medium text-stone-700">EPUB 파일</span>
      <input class="block w-full rounded-2xl border border-stone-300 bg-stone-50 px-4 py-3 text-sm text-stone-900" type="file" name="epubFile" accept=".epub,application/epub+zip" required />
    </label>

    <button class="inline-flex items-center rounded-full bg-stone-900 px-4 py-2 text-sm font-medium text-white no-underline transition hover:bg-stone-700" type="submit">Upload EPUB</button>
  </form>
</section>
